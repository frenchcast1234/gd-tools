# LocalStorage system

> The javascript local storage, but better

You can save string, number and boolean values.  
The variable `path` set the json file which save the data. If you change it, you lose the data, but the file is still existing.  

The function `security` set the default value.

You can use it as you want.
